
@*
    Applies a global Authorization atribute to all pages meaning the user
    must be authenticated to view them. Use the [AllowAnonymous] attribute on
    a per-page basis to override this functionality as this is most secure.
*@
@attribute [Authorize]

@layout MainLayout

@* 
    TODO: Can we actually get away with leaving this in 100% of the time?
    @attribute [StreamRendering] 
*@