{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "SendGrid": {
    "ApiKey": "Use `dotnet user-secrets set SendGrid:ApiKey <API_KEY>` to override this value.",
    "EmailFromAddress": "Use `dotnet user-secrets set SendGrid:EmailFromAddress <EMAIL>` to override this value."
  }
}
