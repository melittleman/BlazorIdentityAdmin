<CascadingValue Value="false" Name="UsePopoverProvider">

    <MudThemeProvider @bind-IsDarkMode="@_isDarkMode" Theme="CustomTheme" DefaultScrollbar="false" />
</CascadingValue>

@*
    TODO: How are we going to pass this in? Ideally this component needs to be 'InteractiveServer'?
    <MudSwitch @bind-Value="@_isDarkMode" Color="Color.Primary" Class="ma-4" T="bool" Label="Toggle Light/Dark Mode" /> 
*@

@code {

    private bool _isDarkMode = true;

    // TODO: Where do we want to get this from?
    public static MudTheme CustomTheme => new()
    {
        Palette = new PaletteLight()
        {
            Primary = "#262937",
            Secondary = "#f4b43a",
            DrawerBackground = "#262937",
            AppbarBackground = "#32333d",
            DrawerText = "#b2b0bf"
        },
        PaletteDark = new PaletteDark()
        {
            Primary = "#262937",
            Secondary = "#f4b43a",
            DrawerBackground = "#262937",
            AppbarBackground = "#32333d",
            DrawerText = "#b2b0bf"
        },
        ZIndex = new ZIndex()
        {
            // This forces the Drawer to open over the top off 
            // everything else including the app header bar.
            Drawer = 9999
        }
    };
}