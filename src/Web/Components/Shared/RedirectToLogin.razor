@inject NavigationManager NavigationManager

@code {

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/login?return_url={Uri.EscapeDataString(NavigationManager.Uri)}", forceLoad: true);
    }
}
